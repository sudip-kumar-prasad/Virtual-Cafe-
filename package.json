{
  "name": "virtual-cafe",
  "version": "1.0.0",
  "description": "Full-stack Virtual Cafe application with React frontend and Express/MongoDB backend",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "install-root": "npm install",
    "setup": "npm install && npm run install-all",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "keywords": [
    "cafe",
    "restaurant",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC"
}